{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "schema_id": "copernicus:clms:ua-stl",
  "schema_version": "1.0.0",
  "status": "current",
  "description": "Copernicus Urban Atlas Street Tree Layer filename per the CLMS Filenaming Framework (extension optional).",
  "fields": {
    "programme": {
      "type": "string",
      "enum": [
        "CLMS"
      ],
      "description": "Constant programme prefix"
    },
    "product": {
      "type": "string",
      "enum": [
        "UA"
      ],
      "description": "Product code"
    },
    "variable": {
      "type": "string",
      "enum": [
        "STL"
      ],
      "description": "Street Tree Layer"
    },
    "survey": {
      "type": "string",
      "pattern": "^(?:S\\d{4}|C\\d{4}-\\d{4})$",
      "description": "Temporal coverage token (e.g., S2021 or C2018-2021)"
    },
    "type": {
      "type": "string",
      "enum": [
        "VEC"
      ],
      "description": "Data type vector",
      "stac_map": {
        "values": {
          "V": {
            "representation": "vector"
          }
        }
      }
    },
    "area_code": {
      "type": "string",
      "pattern": "^[A-Z]{2}\\d{3}L[A-Za-z0-9]$",
      "description": "Functional urban area (FUA) code"
    },
    "city": {
      "type": "string",
      "pattern": "^[\\w-]+$",
      "description": "City or metropolitan area identifier (letters, digits and hyphen)"
    },
    "epsg_code": {
      "type": "string",
      "pattern": "^\\d{4}$",
      "description": "Bare EPSG code, no zero-padding (e.g., 3035)"
    },
    "production_date": {
      "type": "string",
      "pattern": "^\\d{8}$",
      "description": "Production date (YYYYMMDD)"
    },
    "extension": {
      "type": "string",
      "pattern": "^[A-Za-z0-9]+(?:\\.[A-Za-z0-9]+)?$",
      "description": "Optional file extension without leading dot (allows compound extensions such as gpkg.zip)"
    },
    "version_revision": {
      "type": "string",
      "pattern": "^V\\d{2}-R\\d{2}$",
      "description": "Single hyphenated version+revision (R=Revision)"
    }
  },
  "template": "{programme}_{product}-{variable}_{survey}_{type}_{area_code}_{city}_{epsg_code}_{version_revision}_{production_date}[.{extension}]",
  "examples": [
    "CLMS_UA-STL_S2021_V025ha_DK004L3_AALBORG_3035_V01-R00_20240212"
  ]
}